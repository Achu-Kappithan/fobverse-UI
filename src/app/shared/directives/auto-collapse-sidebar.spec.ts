import { AutoCollapseSidebar } from './auto-collapse-sidebar';

describe('AutoCollapseSidebar', () => {
  it('should create an instance', () => {
    const directive = new AutoCollapseSidebar();
    expect(directive).toBeTruthy();
  });
});
